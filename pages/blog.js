import Layout from '../components/Layout';
import BlogPage from '../components/BlogPage';

const Blog = (props) => {
    return (
        <Layout>
            <BlogPage />
        </Layout>
    );
};

export default Blog;
