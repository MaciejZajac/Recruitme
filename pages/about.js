import Layout from '../components/Layout'
import AboutPage from '../components/AboutPage'

const About = (props) => {
    return (
        <Layout>
            <AboutPage />
        </Layout>
    )
}

export default About
